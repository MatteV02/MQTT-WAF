version: "1.0"
default_policies:
  publish: "allow"
  subscribe: "allow"
rules:
  message:
    - id: "MSG-001"
      name: "SQLi Protection"
      action: "deny"
      topics:
        - "sensors/data"
      payload_regex: "(DROP TABLE|SELECT \\*)"
      log: true
      invert_match: false